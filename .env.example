# ---- Snapshot generator — run on VPN/LAN, uses the INTERNAL BC address ----
# These connection values are confirmed working from your published services.

BC_BASE=http://10.253.99.143:7048/ERP/ODataV4
BC_COMPANY=KIFIYA FINANCIAL TECHNOLOGY

# Use a dedicated READ-ONLY BC user + its Web Service Access Key.
# (Rotate the TESTESS credential from the old Postman collection — don't reuse it.)
BC_USER=your-readonly-user
BC_KEY=your-web-service-access-key

# Internal address is HTTP, so cert verification doesn't apply. Leave false.
BC_ALLOW_SELF_SIGNED=false
